1. Write a recursive function to find the factorial of a given number (n! where n is a non-negative integer). 

2. Implement MergeSort in C++, accepting an array of integers as input. Exchange solutions with your classmates (to learn who wrote the best code). 

